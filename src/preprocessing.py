def preprocess_text(text):
    # converts to lowercase, removes punctuation, removes stopwords, tokenizes
    # returns a cleaned string
    pass

def preprocess_documents(documents):
    # applies preprocess_text on a list of documents
    # returns a list of cleaned strings
    pass