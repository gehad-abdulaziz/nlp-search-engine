def build_tfidf(documents):
    # builds and fits a TF-IDF matrix on the cleaned documents
    # returns the vectorizer and the matrix
    pass

def search_tfidf(query, vectorizer, tfidf_matrix, documents, top_k=5):
    # takes a query string and returns the top_k most similar documents
    # uses cosine similarity
    pass