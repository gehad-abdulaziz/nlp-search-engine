def build_embeddings(documents):
    # encodes all documents using a sentence transformer model
    # returns the model and the embeddings matrix
    pass

def search_embeddings(query, model, embeddings, documents, top_k=5):
    # encodes the query and returns the top_k most similar documents
    # uses cosine similarity on embeddings
    pass